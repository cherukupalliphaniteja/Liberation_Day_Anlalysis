services:
  - type: web
    name: liberation-day-tariff-dashboard
    runtime: python
    plan: free
    region: oregon
    branch: master
    rootDir: liberation-day-replication-main_extension/liberation-day-replication-main
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run dashboard/app.py --server.port $PORT --server.address 0.0.0.0 --server.headless true
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.7
      - key: ANTHROPIC_API_KEY
        sync: false
    healthCheckPath: /_stcore/health
